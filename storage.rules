rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow public read/write for hackathon purposes
      // Note: In production, you would restrict writes to authenticated users
      // or specific paths.
      allow read, write: if true;
    }
  }
}
